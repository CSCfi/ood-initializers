dashboard_layout:
  rows:
    - columns:
      - width: 12
        widgets:
          - logo
    - columns:
      - width: 12
        widgets:
          - motd
    - columns:
      - width: 12
        widgets:
          - pinned_apps
pinned_apps:
  - "sys/shell"
  - "sys/files"
