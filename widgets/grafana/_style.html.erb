<script>
  (function() {
      const style = document.createElement('style');
      style.textContent = `
        .grafana-container {
          display: flex;
          flex-wrap: wrap;
        }
        .grafana-iframe {
          flex: 1 0 100%;
          aspect-ratio: 16/9;
        }
        .grafana-iframe:not(.extra) {
          flex: 1 0 50%;
        }
        .extra-graphs {
          flex-wrap: wrap;
          flex-grow: 1;
          display: flex;
        }
        .hidden {
          position: fixed;
          z-index: -1;
          visibility: hidden;
        }
        .grafana-controls {
          flex-basis: 100%;
          display: flex;
          justify-content: flex-end;
        }
        #show-all-graphs {
          margin-top: 10px;
          font-size: 1.4em;
          color: #595959;
        }
      `;
      document.head.appendChild(style);
    })();
</script>
