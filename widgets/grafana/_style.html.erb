<script>
  (function() {
      const style = document.createElement('style');
      style.textContent = `
        .grafana-container {
            display: flex;
            flex-wrap: wrap;
          }
        .grafana-iframe {
          --cols: 2;
          --col-width: calc(100% / var(--cols));
          flex: 1 0 var(--col-width);
          min-width: var(--col-width);
          height: calc(100vw / var(--cols));
          max-height: 300px;
        }
        @media (min-width: 560px) {
          .grafana-iframe {
            --cols: 4;
          }
        }
        .extra-graphs {
          flex-wrap: wrap;
          flex-grow: 1;
          display: flex;
        }
        .hidden {
            position: fixed;
            z-index: -1;
            visibility: hidden;
        }
        .grafana-controls {
            flex-basis: 100%;
            display: flex;
            justify-content: flex-end;
          }
        #show-all-graphs {
            margin-top: 10px;
            font-size: 1.4em;
            color: #595959;
          }
      `;
      document.head.appendChild(style);
    })();
</script>
